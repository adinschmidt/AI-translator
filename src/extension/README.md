/**
 * This directory contains extension-specific code:
 *
 * - background.ts - Background service worker (MV3)
 * - content.ts - Content script for DOM manipulation
 * - options.ts - Options page logic
 *
 * These are the main entry points that get bundled by build.ts.
 */
