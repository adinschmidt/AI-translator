/**
 * This directory contains shared utilities and types used across the extension:
 *
 * - constants/ - Provider defaults, configuration constants
 * - storage/ - chrome.storage schema types and helper functions
 * - messaging/ - Typed message protocol for runtime communication
 * - utils/ - Common utility functions
 *
 * Code in this directory should not have browser-specific side effects
 * and should be testable in isolation.
 */
